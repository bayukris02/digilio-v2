import { Typography, Card } from 'antd';

const { Title } = Typography;

export default function ReportTablePage() {
  return (
    <div>
      <Title level={4}>Report Table</Title>
      <Card>Report table content will be here</Card>
    </div>
  );
}
