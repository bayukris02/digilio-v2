import { Typography, Card } from 'antd';

const { Title } = Typography;

export default function ReportPivotPage() {
  return (
    <div>
      <Title level={4}>Report Pivot</Title>
      <Card>Report pivot content will be here</Card>
    </div>
  );
}
